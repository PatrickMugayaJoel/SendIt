language: python

python:
  - "3.6.5"
  
before_install:
  - "export PYTHONPATH=$PYTHONPATH:$(pwd)"
  - psql -c 'create database sendit;' -U postgres

script:
  - python coveralls.py test
  - python coveralls.py cov

  sudo: required
  
after_success:
  - coveralls