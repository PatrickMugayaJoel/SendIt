Create a parcel delivery order.
---
tags:
  - user
  - Parcel
  
parameters:
  - in: body
    name: body
    required: true
    type: string
    description: This route is for a user to create a parcel delivery order. 
responses:
  201:
    description: Delivery order successfuly created
    schema:
      id: register
      properties:
        orderid:
          type: integer
          default: 1
        destination:
          type: string 
          default: None
        price:
          type: integer
          default: 0
        pickuplocation:
          type: string
          default: None
        parcelSize:
          type: string
          default: None
        date:
          type: string
          default: None
  400:
    description: Validation error