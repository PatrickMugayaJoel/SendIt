User Signup.
---
tags:
  - Signup
consumes:
 - application/json
produces:
 - application/json
parameters:
  - in: body
    name: body
    required: true
    type: object
    description: This route is for Signing a user up.
    schema:
      required:
        - name
        - username
        - password
      type: object
      properties:
        name:
          type: string 
          example: admin
        username:
          type: string
          example: admin
        password:
          type: string
          example: admin
responses:
  201:
    description: User added successfuly
  400:
    description: Validation error